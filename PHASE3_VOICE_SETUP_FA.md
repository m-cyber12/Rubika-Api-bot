# مرحلهٔ سوم — Voice روبیکا + میکروفن Dashboard

این نسخه تمام قابلیت‌های مرحلهٔ دوم سروری را حفظ می‌کند و ورودی/خروجی صوتی اضافه می‌کند.

## معماری

```text
Voice روبیکا یا میکروفن مرورگر
        ↓
Groq Whisper Large V3 Turbo (STT)
        ↓
Agent + Search + Server Tools + Automation
        ↓
پاسخ متنی + Edge TTS فارسی (MP3)
```

فایل صوتی ورودی در حافظه پردازش می‌شود و روی دیسک Render ذخیره نمی‌شود.

---

## ۱. فایل‌های پروژه

از ZIP مرحلهٔ سوم:

- `bot_phase3_voice.py` را به `bot.py` تغییر نام دهید.
- `requirements_phase3.txt` را به `requirements.txt` تغییر نام دهید.

هر دو فایل باید جایگزین شوند؛ مرحلهٔ سوم بدون `edge-tts` فقط پاسخ متنی خواهد داشت.

---

## ۲. Environment در Render

```env
GROQ_API_KEY=کلید_ساخته‌شده_در_Groq
VOICE_STT_MODEL=whisper-large-v3-turbo
VOICE_LANGUAGE=fa
VOICE_TTS_VOICE=fa-IR-FaridNeural
VOICE_MAX_BYTES=10000000
VOICE_MAX_SECONDS=60
```

صدای زنانه:

```env
VOICE_TTS_VOICE=fa-IR-DilaraNeural
```

کلید Groq را در چت، GitHub، فایل کد یا اسکرین‌شات قرار ندهید.

---

## ۳. بررسی Deploy

```text
https://YOUR-SERVICE.onrender.com/api/health
```

باید نشان دهد:

```json
{"version":"phase3-voice-v1.5-owner-power-control"}
```

پس از ورود به Dashboard:

```text
https://YOUR-SERVICE.onrender.com/api/voice/status
```

باید این دو مقدار فعال باشند:

```json
{"stt":{"configured":true},"tts":{"available":true}}
```

---

## ۴. تست Voice روبیکا

Voice فقط برای GUIDهای موجود در `OWNER_GUIDS` پردازش می‌شود.

در Saved Messages دو ویس جدا بفرستید:

```text
وضعیت سرور رو بگو
```

```text
آخرین خبرهای BBC رو بخون
```

باید ابتدا پاسخ متنی و سپس پاسخ صوتی دریافت کنید.

در گروه، داخل ویس کلمهٔ فعال‌سازی را هم بگویید:

```text
فرایدی، وضعیت سرور رو بگو
```

Voice کاربران غیرمالک بدون دانلود و بدون ارسال به Groq نادیده گرفته می‌شود.

### درخواست پاسخ صوتی از داخل پیام متنی

پیام متنی معمولی فقط پاسخ متنی می‌گیرد. برای دریافت متن + ویس بنویسید:

```text
وضعیت سرور رو با ویس بگو
```

```text
آخرین خبرهای BBC رو برام بخون
```

```text
جوابت رو صوتی بفرست
```

برای لغو صدا در همان پیام:

```text
فقط متن جواب بده و ویس نفرست
```

عبارت‌های منفی بر درخواست صوتی اولویت دارند. این قابلیت هم در روبیکا و هم پیام متنی Dashboard فعال است؛ در Dashboard یک پخش‌کنندهٔ صوت زیر پاسخ نمایش داده می‌شود.

### جست‌وجوی خواندنی روبیکا از Dashboard

```text
تو اکانت روبیکام یه پیوی هست به نام M، پیداش کن
```

این ابزار از `get_chats`، `get_contacts` و `search_global_objects` استفاده می‌کند و نام، نوع، GUID ماسک‌شده و یک `chat_ref` موقت می‌دهد. شماره تلفن و اطلاعات Session نمایش داده نمی‌شوند.

### خواندن پیام با مرجع موقت

بعد از پیدا‌شدن چت، Agent می‌تواند با `chat_ref` پیام‌های اخیر یا متن خاصی را بخواند. هر پیام یک `message_ref` موقت می‌گیرد. متن پیام‌ها دادهٔ غیرقابل اعتماد تلقی می‌شود و دستورهای داخل آن اجرا نمی‌شوند.

### عملیات نوشتنی و حالت تأیید متعادل

حالت پیش‌فرض `destructive_only` است:

- ارسال پیام مستقیم اجرا می‌شود.
- ویرایش، حذف، پین و آن‌پین ابتدا Pending می‌شوند.
- عملیات Pending یک کد هشت‌کاراکتری و پانزده دقیقه اعتبار دارد.

```text
⚠️ عملیات فقط آماده شد و هنوز اجرا نشده است
کد: a1b2c3d4
```

تأیید از پنل «کنترل روبیکا» در Dashboard یا با متن زیر در چت خصوصی مالک ممکن است:

```text
تایید روبیکا a1b2c3d4
```

لغو:

```text
لغو روبیکا a1b2c3d4
```

نمونهٔ روند کامل:

```text
تو روبیکا پیوی به نام M رو پیدا کن
پیام‌های c_ab12cd34 رو نشون بده
به c_ab12cd34 بگو سلام
```

برای مدیریت یک پیام پس از دریافت `message_ref`:

```text
پیام m_ab12cd34 در c_ab12cd34 رو ویرایش کن به متن جدید
پیام m_ab12cd34 در c_ab12cd34 رو حذف کن
پیام m_ab12cd34 در c_ab12cd34 رو پین کن
پیام m_ab12cd34 در c_ab12cd34 رو آن‌پین کن
```

ارسال پیام در حالت فعلی مستقیم است؛ چهار عملیات مدیریت پیام Pending می‌سازند و بدون تأیید اجرا نمی‌شوند.

قوانین امنیتی:

- تأیید داخل گروه رد می‌شود.
- تأیید صوتی رد می‌شود؛ کد باید تایپ شود.
- عملیات دوبار اجرا نمی‌شود.
- ویرایش و حذف فقط برای پیام‌های ارسال‌شده توسط همان حساب مجاز است.
- `chat_ref` بعد از ۷ روز و `message_ref` بعد از ۲۴ ساعت منقضی می‌شود.
- Session، Auth، RSA Private Key، شماره تلفن و GUID کامل در اختیار مدل قرار نمی‌گیرند.
- ارسال رمز، token و API key توسط Agent مسدود است.

### هویت مالک و خاموش/روشن

برای اینکه Agent شما را مخاطب عادی حساب نکند، تنظیم کنید:

```env
OWNER_NAME=نام شما
OWNER_TITLE=رئیس
```

پیام Dashboard و پیام دارای `OWNER_GUIDS` با Trusted Owner Context پردازش می‌شوند و پاسخ‌های مستقیم ابزارها نیز با «رئیس» شروع می‌شوند. سایر مخاطبان این خطاب را دریافت نمی‌کنند.

فرمان‌های خصوصی مالک:

```text
فرایدی خاموش شو
فرایدی روشن شو
وضعیت ربات
```

در حالت خاموش:

- متن و Voice روبیکا نادیده گرفته می‌شوند.
- Automation و ارسال به روبیکا متوقف می‌شود.
- تأیید عملیات روبیکا اجرا نمی‌شود.
- فقط فرمان متنی مالک برای روشن‌شدن پذیرفته می‌شود.
- Dashboard برای مدیریت و روشن‌کردن همچنان باز است.

وضعیت از تب «کنترل روبیکا» Dashboard نیز با دکمه روشن/خاموش قابل تغییر است.

---

## ۵. تست میکروفن Dashboard

Dashboard را با HTTPS باز کنید و وارد تب «چت با AI Agent» شوید.

1. دکمهٔ میکروفن را بزنید.
2. مجوز میکروفن مرورگر را تأیید کنید.
3. صحبت کنید.
4. برای توقف دوباره دکمه را بزنید.

ضبط پس از ۶۰ ثانیه خودکار متوقف می‌شود. Dashboard متن تشخیص‌داده‌شده، پاسخ و پخش‌کنندهٔ صوت را نشان می‌دهد.

مرورگر باید `MediaRecorder` را پشتیبانی کند. Chrome/Edge روی HTTPS پیشنهاد می‌شود.

### Live JARVIS

از تب `Live JARVIS` روی «شروع جلسه» بزنید. تا وقتی صفحه باز است:

- میکروفن پیوسته برای تشخیص سطح صدا فعال است.
- بعد از ۱٫۲ ثانیه سکوت، جمله خودکار ارسال می‌شود.
- هر نوبت حداکثر ۳۰ ثانیه است.
- هنگام پخش جواب، شنیدن میکروفن متوقف می‌شود تا feedback ایجاد نشود.
- دکمه‌های Mute، Interrupt و Stop در دسترس‌اند.

این حالت Turn-based است و از endpoint فعلی Groq/Edge استفاده می‌کند؛ Full-duplex streaming واقعی نیست.

---

## ۶. محدودیت‌ها و امنیت

- حداکثر حجم ورودی پیش‌فرض: ۱۰ مگابایت
- حداکثر زمان Voice روبیکا: ۶۰ ثانیه
- حداکثر دو پردازش هم‌زمان
- حداکثر متن TTS: ۱۵۰۰ کاراکتر
- حداکثر cache: ۱۰ پاسخ صوتی در RAM
- MIME و پسوند فایل بررسی می‌شوند.
- فایل صوتی کاربر روی دیسک ذخیره نمی‌شود.
- اگر TTS شکست بخورد، پاسخ متنی همچنان ارسال می‌شود.
- اگر STT شکست بخورد، ابزار یا Agent اجرا نمی‌شود.

---

## ۷. رفع خطا

### `Groq HTTP 401`

`GROQ_API_KEY` اشتباه یا منقضی است.

### `Groq HTTP 429`

سهمیه یا نرخ Groq محدود شده است. کد یک بار با فاصله کوتاه retry می‌کند و بعد خطای کنترل‌شده می‌دهد.

### پاسخ متن می‌آید ولی ویس نمی‌آید

- `/api/voice/status` را بررسی کنید.
- در لاگ دنبال `VOICE REPLY TTS ERROR` بگردید.
- دسترسی خروجی Render به سرویس Edge TTS ممکن است موقتاً قطع باشد.

### میکروفن Dashboard کار نمی‌کند

- Dashboard باید با HTTPS باز شود.
- مجوز Microphone مرورگر را فعال کنید.
- Chrome/Edge به‌روز استفاده کنید.

### ویس گروه نادیده گرفته می‌شود

- فرستنده باید داخل `OWNER_GUIDS` باشد.
- داخل ویس کلمهٔ «فرایدی» گفته شود.

---

## نکتهٔ حریم خصوصی

Voice برای تبدیل گفتار به متن به Groq ارسال می‌شود و متن پاسخ برای ساخت صوت به سرویس Edge TTS ارسال می‌شود. اطلاعات حساس، رمز، کلید API یا مکالمهٔ خصوصی محرمانه را به‌صورت Voice ارسال نکنید مگر سیاست نگهداری دادهٔ سرویس‌ها را پذیرفته باشید.
