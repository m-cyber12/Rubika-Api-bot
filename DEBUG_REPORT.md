# گزارش دیباگ — `bot.py`

فایل اصلی `bot.py` **دست‌نخورده** باقی مونده. نسخهٔ اصلاح‌شده: **`bot_fixed.py`**
(هرجا تغییر دادم با کامنت `FIX #1` / `FIX #2` علامت زده شده).

روش کار: کتابخانهٔ `rubpy==7.3.5` رو باز کردم، یه کلاینت روبیکای تقلبی (fake) ساختم و
هر دو باگ رو با تست واقعی بازتولید و بعد تأیید کردم.

---

## باگ ۱ — تو گروه، ریپلای به پیام ربات تشخیص داده نمی‌شه

### ریشهٔ مشکل (سه تا اشکال روی هم)

**الف) مقایسهٔ `int` با `str`** — اصلی‌ترین دلیل.

در `bot.py` خط ۵۲۶:
```python
message_id = int(raw_msg_id) if raw_msg_id is not None else None
```
ولی روبیکا `message_id` رو **رشته** می‌ده (مثلاً `"7001"`). پس:

| جا | نوع مقدار |
|---|---|
| `bot_sent_message_ids` بعضی جاها | `int` از `int(raw_msg_id)` |
| `reply_to_message_id` که از آپدیت میاد | همیشه `str` |

و بعد خط ۵۶۳:
```python
is_reply_to_bot = reply_to is not None and reply_to in bot_sent_message_ids
```
`"7001" in {7001}` → `False`. یعنی ربات ریپلای رو **همیشه** رد می‌کرد.

**ب) `bot_sent_message_ids` روی رم و بدون ذخیره.**
یه `set()` ساده تو حافظه‌ست. با هر ری‌استارت (روی Render خیلی هم زیاد اتفاق می‌افته)
خالی می‌شه، پس ریپلای به پیام‌های قبلی ربات دیگه شناخته نمی‌شه.

**ج) خوندن `reply_to_message_id` قابل اعتماد نیست.**
```python
reply_to = getattr(update, "reply_to_message_id", None) or getattr(update, "reply_message_id", None)
```
تو `rubpy`، کلاس `Update` یه `__getattr__` داره که هر اسمی رو تو دیکشنری خام سرچ می‌کنه —
پس معمولاً کار می‌کنه، **ولی** پراپرتی رسمی فقط `reply_message_id` هست و ترتیب `or`
باعث می‌شه به مسیر ناپایدار تکیه کنی.

**د) بونوس:** ربات پیام‌های خودش رو فیلتر نمی‌کرد → خطر حلقهٔ جواب‌به‌خود.

### راه‌حل در `bot_fixed.py`

1. تابع `norm_id()` — همهٔ آیدی‌ها به `str` نرمال می‌شن، دیگه هیچ‌جا `int()` نداریم.
2. `remember_bot_message(sent)` — یه‌جا و درست آیدی پیام ارسالی ربات رو ذخیره می‌کنه
   (هم `getattr` هم مسیر `message_update.message_id`).
3. `bot_sent_message_ids` تو فایل `bot_message_ids.json` ذخیره می‌شه → بعد از ری‌استارت هم هست.
4. `get_reply_to_id(update)` — سه مسیر رو به ترتیب امتحان می‌کنه
   (`reply_message_id` → `reply_to_message_id` → دیکشنری خام).
5. `is_reply_to_me()` — اگه آیدی تو حافظه نبود، با `client.get_messages_by_id()`
   پیام ریپلای‌شده رو از سرور می‌گیره و `author_object_guid` رو با گوییدِ خودمون مقایسه می‌کنه.
   یعنی **حتی با حافظهٔ کاملاً خالی هم ریپلای درست تشخیص داده می‌شه.**
6. `MY_GUID` موقع بوت با `get_me()` پر می‌شه؛ پیام‌های خود ربات نادیده گرفته می‌شن.
7. یه لاگ دیباگ اضافه شد: `[GROUP] reply_to=... is_reply_to_bot=... trigger=...`

### نتیجهٔ تست

| سناریو | `bot.py` | `bot_fixed.py` |
|---|---|---|
| «فرایدی سلام» تو گروه | ✅ جواب می‌ده | ✅ جواب می‌ده |
| ریپلای به پیام ربات (بدون تریگر) | ⚠️ فقط تو همون ران | ✅ |
| ریپلای بعد از ری‌استارت | ❌ ساکت | ✅ (از سرور چک می‌کنه) |
| ریپلای به پیام یکی دیگه | ✅ ساکت | ✅ ساکت |

---

## باگ ۲ — تو تب «سوالات»، سوال‌ها نمیاد

### ریشهٔ مشکل

**الف) کلید `pending_replies` وسط کار از `int` به `str` عوض می‌شه.**

موقع ثبت (خط ۶۳۴)، کلید `int` هست:
```python
pending_id = random.randint(100000, 999999)   # int
pending_replies[pending_id] = {...}
```
ولی بلافاصله بعدش با آیدی پیام گروه کنترل جایگزین می‌شه که **`str`** هست:
```python
nid = getattr(sent_notif, "message_id", None)   # "7004"  ← رشته
pending_replies[nid] = pending_replies.pop(pending_id)
```

**ب) `load_all()` هر کلید غیرعددی رو دور می‌ریزه** (خطوط ۹۰–۹۵):
```python
for k, v in pending_raw.items():
    try:
        pending_replies[int(k)] = v
    except:
        pass          # ← بی‌صدا حذف می‌شه
```
اگه آیدی روبیکا رقمی نباشه یا خیلی بلند باشه، سوال **بی‌سروصدا ناپدید می‌شه**.
بعد از هر ری‌استارت لیست سوالات خالی می‌شد.

**ج) `/api/answer` همیشه ۴۰۴ می‌داد.**
```python
pid = data.get("id")            # int از parseInt
if pid not in pending_replies:  # کلیدها str بودن → همیشه Not found
```
تست واقعی: کلید `"7004"` تو حافظه، درخواست با `id=7004` → **`404 Not found`**.

**د) سمت جاوااسکریپت `parseInt(id)`** — روی آیدی‌های بلند روبیکا خرابش می‌کرد.

**ه) الگوهای «نمی‌دونم» ناقص بودن** → خیلی از سوال‌ها اصلاً وارد صف نمی‌شدن:
```
"اطلاعی ندارم"     → تشخیص نداده می‌شد ❌
"خبر ندارم"        → ❌
"اطلاع دقیقی ندارم" → ❌
"یادم نیست"        → ❌
```
(فقط `اطلاع[ات\s]+ندارم` بود که «اطلاعی ندارم» رو نمی‌گرفت.)

### راه‌حل در `bot_fixed.py`

1. کلید pending **همه‌جا `str`** — موقع ثبت، ذخیره، لود و در API.
2. `load_all()` دیگه چیزی رو دور نمی‌ریزه.
3. `api_answer()` ورودی رو با `norm_id()` نرمال می‌کنه → دیگه ۴۰۴ الکی نمی‌ده.
4. جاوااسکریپت `String(id)` می‌فرسته به‌جای `parseInt(id)`.
5. `WAITING_PATTERNS` گسترش پیدا کرد (۱۳ الگو به‌جای ۷) + تابع `looks_like_waiting()`.
6. `/api/pending` جدیدترین سوال رو بالا نشون می‌ده و `count` هم برمی‌گردونه.
7. تب سوالات خودکار رفرش می‌شه (فقط وقتی تعداد عوض شده، تا متن در حال تایپ پاک نشه).
8. **بونوس:** وقتی تو گروه کنترل ریپلای می‌زنی، جواب علاوه بر ذخیره تو دانش،
   برای همون کسی که پرسیده بود هم فرستاده می‌شه (قبلاً فقط ذخیره می‌شد و طرف هیچ‌وقت جواب نمی‌گرفت).

---

## یه باگ خفته که موقع بررسی پیدا شد

خط ۵۰۳ در `bot.py`:
```python
def get_chat_session(chat_guid):
    if chat_guid not in chat_histories:
        chat_histories[chat_guid] = model.start_chat(history=[])   # NameError
```
`model` هیچ‌جای فایل تعریف نشده. چون این تابع هیچ‌جا صدا زده نمی‌شه فعلاً کرش نمی‌کنه،
ولی به‌محض استفاده `NameError` می‌ده. در نسخهٔ اصلاح‌شده درست شد.

---

## فایل‌های جدیدی که ساخته می‌شن

علاوه بر فایل‌های قبلی، `bot_fixed.py` این رو هم می‌سازه:

- `bot_message_ids.json` — آیدی پیام‌های ارسالی ربات (برای تشخیص ریپلای بعد از ری‌استارت)

> نکته برای Render: فایل‌سیستم موقتیه. `is_reply_to_me()` عمداً fallback سروری داره
> تا حتی اگه این فایل پاک شد، ریپلای درست کار کنه.

## اجرا

```bash
python bot_fixed.py
```
یعنی هیچ وابستگی جدیدی لازم نیست؛ همون `requirements.txt` فعلی کافیه.
